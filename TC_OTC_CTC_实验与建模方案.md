# TC / OTC / CTC 定性判别实验与建模方案

## 1. 目标概述

本项目的目标不是定量求出 `TC`、`OTC`、`CTC` 三种物质在待测液中的具体浓度，而是基于探针体系在不同缓冲条件下的颜色响应差异，完成如下定性判别：

- 仅存在 `TC`、`OTC`、`CTC` 中的一种
- 同时存在其中两种
- 三种同时存在
- 空白样品

当前已知事实如下：

- 探针体系为 `1800 缓冲 + 200 探针`
- 在比色皿条件下，加入 `TC`、`OTC`、`CTC` 后，`0~80 μM` 范围内颜色变化不同
- 在不同缓冲条件下，同一样品的颜色变化模式也不同
- 可以在黑匣子内、固定手机、固定曝光、固定焦距、固定拍摄距离下采集比色皿图像
- 可以从图像中提取比色皿液体区域的平均 `RGB`

因此，本项目采用“四缓冲联合 RGB 响应”来判别物质存在性，而不是依赖单一缓冲、单一颜色值。

## 2. 总体路线

整体方案分为两个实验阶段和两个数据处理模块：

### 2.1 第一阶段：固定总浓度概念验证

目的：

- 先验证四缓冲下的联合 `RGB` 是否足以区分不同存在模式
- 证明这条路线本身可行

设定：

- 固定总浓度：`60 μM`
- 存在阈值：`10%`
- 对应最低存在浓度：`6 μM`

在该阶段，只要某组分浓度 `>= 6 μM`，就认为该组分“存在”。

### 2.2 第二阶段：扩展到多个总浓度水平

目的：

- 让模型学会在总浓度变化时，仍然稳定判断 `TC` / `OTC` / `CTC` 是否存在
- 使方法更接近真实未知样品场景

这一阶段不改变判别目标，只扩展训练覆盖范围。

### 2.3 图像处理模块

目的：

- 自动识别比色皿
- 自动截取液体核心区域
- 自动输出平均 `RGB`
- 自动生成标注预览图，便于核查

### 2.4 模型判别模块

目的：

- 输入四种缓冲下的 `RGB`
- 输出 `TC`、`OTC`、`CTC` 三个物质各自“存在 / 不存在”的判断

## 3. 为什么采用多标签判别而不是 7 分类

本项目的最终问题不是“这 7 个类别谁赢了”，而是分别判断三种物质是否存在：

- `TC` 是否存在
- `OTC` 是否存在
- `CTC` 是否存在

因此主模型应采用“多标签分类”思路，而不是单一 `7` 分类。

推荐输出形式：

- `tc_present`
- `otc_present`
- `ctc_present`

例如：

- `TC only` -> `(1,0,0)`
- `OTC only` -> `(0,1,0)`
- `CTC only` -> `(0,0,1)`
- `TC+OTC` -> `(1,1,0)`
- `TC+CTC` -> `(1,0,1)`
- `OTC+CTC` -> `(0,1,1)`
- `TC+OTC+CTC` -> `(1,1,1)`
- `blank` -> `(0,0,0)`

这样做的原因：

- 更符合化学意义
- 更便于扩展到第二阶段
- 更容易解释模型输出
- 对未知样品的存在性判断更直接

## 4. 第一阶段实验设计

## 4.1 第一阶段核心设定

- 固定总浓度：`60 μM`
- 存在阈值：`10%`
- 最低存在浓度：`6 μM`
- 每种配方做 `3` 个独立重复

这里的“独立重复”指重新配制样品，而不是同一样品重复拍照。

## 4.2 第一阶段样品分组

第一阶段按存在模式分为四类：

- `blank`
- `single`
- `binary`
- `ternary`

### 4.2.1 空白组

- `0 / 0 / 0`

### 4.2.2 单组分组

- `60 / 0 / 0`
- `0 / 60 / 0`
- `0 / 0 / 60`

### 4.2.3 二元混合组

`TC + OTC`：

- `54 / 6 / 0`
- `48 / 12 / 0`
- `42 / 18 / 0`
- `30 / 30 / 0`
- `18 / 42 / 0`
- `12 / 48 / 0`
- `6 / 54 / 0`

`TC + CTC`：

- `54 / 0 / 6`
- `48 / 0 / 12`
- `42 / 0 / 18`
- `30 / 0 / 30`
- `18 / 0 / 42`
- `12 / 0 / 48`
- `6 / 0 / 54`

`OTC + CTC`：

- `0 / 54 / 6`
- `0 / 48 / 12`
- `0 / 42 / 18`
- `0 / 30 / 30`
- `0 / 18 / 42`
- `0 / 12 / 48`
- `0 / 6 / 54`

### 4.2.4 三元混合组

- `48 / 6 / 6`
- `6 / 48 / 6`
- `6 / 6 / 48`
- `36 / 18 / 6`
- `36 / 6 / 18`
- `18 / 36 / 6`
- `6 / 36 / 18`
- `18 / 6 / 36`
- `6 / 18 / 36`
- `20 / 20 / 20`

### 4.2.5 第一阶段总规模

- 空白：`1` 种
- 单组分：`3` 种
- 二元混合：`21` 种
- 三元混合：`10` 种

合计：

- `35` 种唯一配方
- 每种 `3` 个独立重复
- 共 `105` 个样品记录

## 5. 图像采集要求

为了保证 `RGB` 可比性，拍摄条件必须尽可能固定：

- 黑匣子内拍摄
- 同一手机
- 同一曝光
- 同一焦距
- 同一拍摄距离
- 同一背景条件
- 比色皿放置方式尽量一致

当前拍摄体系已经满足较高标准化，因此第一阶段后续分析可优先使用原始 `RGB`。

注意：

- 虽然模型输入可直接使用原始 `RGB`，但建议保留空白图像或空白样品，便于后续排查底色漂移
- 若后续发现不同批次之间存在系统性偏移，可再考虑做空白校正或颜色归一化

## 6. 自动 RGB 提取程序的定位

本项目后续需要一个自动程序来替代人工取色。

程序职责建议限定为：

- 批量读取原始照片
- 自动识别比色皿位置
- 自动截取液体核心区域
- 计算该区域平均 `RGB`
- 输出标注预览图
- 输出结果表

当前不要求程序自动解析样品类别、缓冲类型、浓度信息，这些可以后期手动补充。

### 6.1 为什么不直接取整个比色皿平均色

不建议直接对整个比色皿框求平均 `RGB`，原因如下：

- 比色皿边框存在明显高亮反光
- 顶部液面弯月面会拉偏颜色
- 底部反射和边缘高亮不代表样品主体颜色

因此应优先截取液体中部或中下部的稳定核心区域作为取色区。

### 6.2 自动识别思路

基于当前图像特征，自动识别可以利用以下特点：

- 黑背景下比色皿是明显亮区
- 比色皿形状接近竖直矩形
- 位置和尺寸变化不大
- 红色文字与白色水印虽然显眼，但在位置、长宽比、连通形状上与比色皿差异较大

程序最终建议输出：

- 原始图片路径
- 比色皿外框坐标
- 取色区域坐标
- 平均 `RGB`
- 可选标准差
- 标注预览图

后续第一版可先做为 Python 脚本，后续再封装界面。

## 7. 第一阶段最小记录表

原始记录表建议使用以下列名：

- `sample_name`
- `group_type`
- `replicate_id`
- `tc_conc_uM`
- `otc_conc_uM`
- `ctc_conc_uM`
- `water_rgb`
- `tris6_rgb`
- `tris7_rgb`
- `tris8_rgb`

说明：

- `water_rgb` 等字段内容采用 `R,G,B` 单列形式记录
- 例如：`123,145,201`
- `group_type` 建议统一使用：
  - `blank`
  - `single`
  - `binary`
  - `ternary`

推荐的 `sample_name` 命名风格：

- `single_TC60_OTC0_CTC0`
- `binary_TC48_OTC12_CTC0`
- `ternary_TC20_OTC20_CTC20`

`replicate_id` 推荐使用：

- `R1`
- `R2`
- `R3`

## 8. 第一阶段完成后的数据整理

假设第一阶段拍照和原始记录已经完成，后续处理建议按以下顺序进行。

### 8.1 拆分 RGB 字段

将以下字段从字符串拆为数值型特征：

- `water_rgb`
- `tris6_rgb`
- `tris7_rgb`
- `tris8_rgb`

拆分后得到 `12` 个输入特征：

- `water_R`
- `water_G`
- `water_B`
- `tris6_R`
- `tris6_G`
- `tris6_B`
- `tris7_R`
- `tris7_G`
- `tris7_B`
- `tris8_R`
- `tris8_G`
- `tris8_B`

### 8.2 生成存在性标签

根据浓度自动生成三列标签：

- `tc_present`
- `otc_present`
- `ctc_present`

判定规则：

- 浓度 `>= 6 μM` -> `1`
- 浓度 `< 6 μM` -> `0`

### 8.3 生成组合标签

为了后续作图和汇总，可额外生成一列：

- `label_combo`

例如：

- `blank`
- `TC`
- `OTC`
- `CTC`
- `TC+OTC`
- `TC+CTC`
- `OTC+CTC`
- `TC+OTC+CTC`

该列主要用于可视化与汇报，不作为主模型的唯一目标。

## 9. 数据质控与注意事项

在正式训练模型之前，必须先做数据质控。

### 9.1 RGB 格式检查

需要确认：

- 每个 `rgb` 字段都能正确拆成 `3` 个数值
- 不存在空值
- 不存在缺项、错项、非数值字符

### 9.2 重复稳定性检查

同一 `sample_name` 下的 `R1 / R2 / R3` 应尽量接近。

若发现某一重复明显偏离另外两个，应优先回查：

- 原始图片
- 取色区域是否偏移
- 是否存在气泡、液面倾斜、明显反光
- 是否发生曝光异常

### 9.3 空白组稳定性检查

空白组应具备较好的集中性。如果空白本底在四缓冲下漂移很大，说明实验体系或拍摄体系还存在不稳定因素。

### 9.4 自动识别结果抽查

建议抽查每组中的部分标注预览图，确认程序确实取到了液体核心区域，而不是：

- 比色皿边框
- 顶部弯月面
- 高光反射区
- 图片中的文字区域

## 10. 第一阶段探索性分析

在上模型之前，先做探索性分析。其目的不是追求最好分数，而是回答：

“四缓冲联合 RGB 是否真的能把不同存在模式拉开？”

建议进行以下分析。

### 10.1 重复稳定性分析

观察同一配方的 `3` 个重复在 `12` 维特征上是否接近。

若重复之间离散度很大，说明体系噪声较高，模型效果即使看起来不错，也可能不稳。

### 10.2 单特征分布分析

逐一观察不同特征对组别的区分能力，例如：

- `water_R`
- `tris6_G`
- `tris7_B`
- `tris8_B`

用于初步寻找敏感通道和敏感缓冲。

### 10.3 PCA 等降维可视化

将 `12` 维 `RGB` 投影到二维空间，观察：

- 单组分之间是否分开
- 二元混合是否形成合理分布
- 三元混合是否有相对稳定区域
- 空白是否与样品明显分离

### 10.4 各组平均 RGB 图谱

按 `label_combo` 或按 `tc_present / otc_present / ctc_present` 分组，绘制平均 `RGB` 图谱，用于总结规律。

## 11. 第一阶段模型训练方案

## 11.1 主模型思路

第一阶段不建议直接做深度学习。由于样本量较小，更适合小样本监督学习。

推荐采用“三个并行二分类器”的结构：

- 模型 A：判断 `TC` 是否存在
- 模型 B：判断 `OTC` 是否存在
- 模型 C：判断 `CTC` 是否存在

输入相同，均为 `12` 个 `RGB` 特征。

### 11.2 推荐模型

优先尝试以下模型：

- `Logistic Regression`
- `Random Forest`

推荐原因：

- `Logistic Regression` 适合做线性基线模型，稳定、易解释
- `Random Forest` 可以捕捉非线性关系，通常对颜色判别更有弹性

两者若结果一致，说明判别较可靠。

### 11.3 为什么不建议 7 分类作为主模型

虽然也可以把样品直接看作：

- `blank`
- `TC`
- `OTC`
- `CTC`
- `TC+OTC`
- `TC+CTC`
- `OTC+CTC`
- `TC+OTC+CTC`

但这不是最优主方案。原因是：

- `TC+OTC` 与 `TC+OTC+CTC` 并非完全独立概念
- 多标签结构更符合实际问题
- 第二阶段扩展到不同总浓度时，多标签方式更自然

## 12. 训练集与验证集划分原则

这一部分非常关键，不能简单随机划分。

### 12.1 禁止重复泄漏

同一 `sample_name` 的不同重复不应分别落入训练集和测试集。

例如：

- `binary_TC48_OTC12_CTC0` 的 `R1`
- `binary_TC48_OTC12_CTC0` 的 `R2`
- `binary_TC48_OTC12_CTC0` 的 `R3`

应被视为同一配方组。

### 12.2 正确做法

应按 `sample_name` 分组进行交叉验证，也就是使用“按配方分组”的验证方式。

这样测试集中的样品配方对于模型来说是“未见过的配方点”，更接近真实泛化能力。

## 13. 模型评价指标

建议每个标签模型分别输出以下指标：

- `accuracy`
- `precision`
- `recall`
- `F1-score`

其中更值得关注的是：

- `precision`：避免误报
- `recall`：避免漏检
- `F1-score`：综合平衡两者

建议第一阶段的目标不是盲目追求极高准确率，而是希望三个标签模型都达到稳定、均衡的较好水平。

## 14. 模型解释与判别依据

本项目最终希望达到“模型主判定 + 规则辅助解释”，因此训练完成后不能只停留在分数层面。

### 14.1 特征重要性

应分析哪些 `RGB` 特征对不同标签最重要，例如：

- 某个缓冲下的蓝通道是否对 `CTC` 特别敏感
- 某个缓冲下的红通道和绿通道组合是否对 `TC` 更关键

### 14.2 均值差异分析

可比较：

- `TC 存在` 与 `TC 不存在`
- `OTC 存在` 与 `OTC 不存在`
- `CTC 存在` 与 `CTC 不存在`

在不同缓冲下的平均 `RGB` 差异。

### 14.3 辅助规则提炼

在主模型之外，可以额外用浅层决策树或人工归纳方法，总结辅助判断规律。

例如未来可能形成类似的表达：

- 在某缓冲下蓝通道明显升高，更支持某物质存在
- 不同缓冲之间的相对变化模式更能区分某些组合

这里的规则不一定替代主模型，但适合用于解释、汇报和论文写作。

## 15. 第一阶段是否成功的判断标准

第一阶段建议满足以下三点后，再进入第二阶段：

### 15.1 模型表现稳定

三个标签模型都具有较好的分类性能，不应只靠某一个标签表现突出。

### 15.2 重复一致性良好

同一配方的多个独立重复应保持相对稳定，不应出现大幅漂移。

### 15.3 解释规律与模型结论一致

特征重要性、均值图谱、PCA 分布等应能互相印证，而不是完全依赖黑箱结果。

如果这三点都成立，则说明：

- 探针体系确实具有区分能力
- 四缓冲联合响应是有效信息源
- 可以继续扩展到第二阶段

## 16. 第二阶段扩展思路

第二阶段的目标是让模型适应不同总浓度，而不是改变判别目标。

### 16.1 推荐做法

在保持第一阶段整体分析流程不变的前提下，增加多个总浓度水平，例如：

- `20 μM`
- `40 μM`
- `60 μM`
- `80 μM`

在每个总浓度下继续设计代表性单组分、二元混合、三元混合样品。

### 16.2 第二阶段模型目标不变

仍然输出：

- `tc_present`
- `otc_present`
- `ctc_present`

而不是预测具体浓度。

### 16.3 第二阶段更严格的验证方式

除了按配方分组外，建议增加“留出某个总浓度只做测试”的验证，以判断模型是否真正学会了存在性，而不是记住了某个总浓度的色深。

## 17. 最终未知样品判定流程

当第一阶段和第二阶段都完成后，未知样品的判定流程如下：

1. 取未知样品
2. 在四种缓冲条件下分别与探针体系反应
3. 分别拍照
4. 使用自动程序提取四组 `RGB`
5. 将四组 `RGB` 送入训练好的三个标签模型
6. 分别得到：
   - `P(TC存在)`
   - `P(OTC存在)`
   - `P(CTC存在)`
7. 按设定阈值转为“存在 / 不存在”
8. 输出最终结论

例如：

- `TC：存在`
- `OTC：不存在`
- `CTC：存在`

则结论为：

- 样品中存在 `TC` 与 `CTC`

## 18. 关键注意事项汇总

为避免实验和建模过程中出现偏差，应重点注意以下问题：

- 不要将目标误设为定量，本项目当前阶段的核心是定性存在性判别
- 第一阶段的重点是验证路线是否成立，不必一开始就追求覆盖所有情况
- 不要只依赖单一缓冲，应始终联合四缓冲 `RGB`
- 不要只看单张照片，应以结构化数据为分析对象
- 不要让同一配方的重复泄漏到训练集和测试集两边
- 不要直接以整个比色皿区域平均色代替液体核心区域颜色
- 不要仅以模型准确率作为结论，应同时看解释性分析结果
- 若后续发现系统性漂移，应补充空白校正或批次修正

## 19. 当前建议的执行顺序

按当前讨论结果，推荐的完整执行顺序如下：

1. 完成第一阶段样品配制与拍照
2. 自动提取四缓冲下的 `RGB`
3. 整理为标准记录表
4. 拆分 `RGB` 字段，生成 `12` 维特征
5. 生成 `tc_present / otc_present / ctc_present`
6. 进行数据质控
7. 进行探索性分析
8. 训练三个并行分类模型
9. 分析特征重要性与辅助规则
10. 判断第一阶段是否成功
11. 若成功，再扩展到第二阶段多个总浓度
12. 最终建立未知样品判定流程

## 20. 当前方案的核心结论

本项目最核心的结论可以概括为以下几点：

- 利用四种缓冲条件下的联合 `RGB` 响应，有希望实现 `TC / OTC / CTC` 的定性存在性判别
- 第一阶段应先在固定总浓度 `60 μM` 下做概念验证
- 判别任务应建模为三个并行的“存在 / 不存在”问题
- 自动化图像取色程序是后续数据处理的重要基础设施
- 第一阶段成功后，再扩展到多个总浓度，才更接近真实未知样品应用


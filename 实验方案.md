# TC / OTC / CTC 定性判别实验与建模方案

## 1. 目标概述

本项目的目标不是定量求出 `TC`、`OTC`、`CTC` 三种物质在待测液中的具体浓度，而是基于探针体系在不同缓冲条件下的颜色响应差异，完成如下定性判别：

- 仅存在 `TC`、`OTC`、`CTC` 中的一种
- 同时存在其中两种
- 三种同时存在
- 空白样品

当前已知事实如下：

- 探针体系为 `1800 缓冲 + 200 探针`
- 在比色皿条件下，加入 `TC`、`OTC`、`CTC` 后，`0~80 μM` 范围内颜色变化不同
- 在不同缓冲条件下，同一样品的颜色变化模式也不同
- 可以在黑匣子内、固定手机、固定曝光、固定焦距、固定拍摄距离下采集比色皿图像
- 可以从图像中提取比色皿液体区域的颜色特征，当前工程化实现已支持 `RGB / HSV / Lab`

因此，本项目采用“多缓冲联合颜色特征响应”来判别物质存在性，而不是依赖单一缓冲、单一颜色值。

## 2. 总体路线

整体方案分为两个实验阶段和两个数据处理模块：

### 2.1 第一阶段：固定总浓度概念验证

目的：

- 先验证多缓冲下的联合颜色特征是否足以区分不同存在模式
- 证明这条路线本身可行

设定：

- 固定总浓度：`60 μM`
- 存在阈值：`10%`
- 对应最低存在浓度：`6 μM`

在该阶段，只要某组分浓度 `>= 6 μM`，就认为该组分“存在”。

### 2.2 第二阶段：扩展到多个总浓度水平

目的：

- 让模型学会在总浓度变化时，仍然稳定判断 `TC` / `OTC` / `CTC` 是否存在
- 使方法更接近真实未知样品场景

这一阶段不改变判别目标，只扩展训练覆盖范围。

### 2.3 图像处理模块

目的：

- 自动识别比色皿
- 自动截取液体核心区域
- 自动输出颜色特征统计
- 自动生成标注预览图，便于核查

### 2.4 模型判别模块

目的：

- 输入多个缓冲下的联合颜色特征
- 输出 `TC`、`OTC`、`CTC` 三个物质各自“存在 / 不存在”的判断

### 2.5 当前缓冲策略更新

原始方案是按“多缓冲联合颜色响应”来规划的，但当前实际推进中，已经将缓冲设计更新为“多缓冲可扩展框架”：

- 后续不强制固定为 `4` 种缓冲
- 可能最终采用 `3` 种缓冲
- 也可能仍然采用 `4` 种缓冲
- 最终以区分效果、重复稳定性和建模表现为准

因此，当前实验与程序设计原则更新为：

- 先以 `water` 这一种缓冲跑通图像处理与数据流程
- 后续逐步接入其他候选缓冲
- 程序和数据表结构都必须支持“缓冲数量不写死”

### 2.6 当前软件实现状态

为了支撑后续多缓冲实验，当前已经同步搭建本地图像处理工具，使用方案如下：

- 开发语言：`Python`
- GUI：`Tkinter`
- 图像处理：`OpenCV + Pillow + NumPy`
- 数据导出：`pandas`
- 环境：`Distinguish_TC` Conda 虚拟环境

当前程序已经实现到以下阶段：

- 扫描 `Photo/` 原始实验图片
- 解析图片文件名中的缓冲名、组别、浓度和重复编号
- 批量自动识别比色皿
- 正式保存比色皿裁剪图
- 自动生成异常待修正列表
- 在原图上修正异常裁剪框
- 在裁剪图上标注 ROI
- 基于裁剪图批量提取 `RGB / HSV / Lab` 颜色统计
- 保存项目状态并可重新打开
- GUI 当前默认显示 `RGB + HSV` 摘要，`Lab` 保留在提取层与实验层

当前实际图像处理流程已经不再采用“原图直接取色”，而是更新为：

1. 原图自动识别比色皿
2. 正式保存比色皿裁剪图
3. 只修正异常裁剪图
4. 在裁剪图上标注 ROI
5. 基于裁剪图提取颜色特征

这意味着后续实验数据整理时，应默认以“裁剪图 + 颜色特征结果表”为图像处理基础，而不是直接以原始照片为最终输入。

### 2.7 当前已完成的四缓冲闭环

在前述“多缓冲可扩展框架”的原则下，当前已经实际完成 `water`、`T=8`、`T=5` 与 `T=10` 四个缓冲的数据整理与基线训练闭环：

- `water` 的提取结果位于 `project_data/rgb_results/rgb_results.csv`
- `T=8` 的提取结果位于 `project_data_t8/rgb_results/rgb_results.csv`
- `T=5` 的提取结果位于 `project_data_t5/rgb_results/rgb_results.csv`
- `T=10` 的提取结果位于 `project_data_t10/rgb_results/rgb_results.csv`
- 四个缓冲的数据已按 `sample_name + replicate_id` 对齐
- 已导出统一长表、训练宽表与质控表
- 已跑通一次基于四缓冲联合颜色特征的多标签基线训练
- 颜色特征融合实验结果位于 `model_runs/color_feature_experiments_20260601_113537/`
- `T=10` 中曾出现的 `RGB` 标准差异常样本已经过手动修正并重新导出，当前结果表无警告项

这一轮的意义不在于一次性得到最终模型，而在于确认以下事实已经成立：

- 四缓冲输入可以稳定进入统一训练表
- 训练集与测试集可以按 `sample_name` 分组切分，避免重复泄漏
- 当前工程已经具备继续接入更多候选缓冲并做特征实验的能力

## 3. 为什么采用多标签判别而不是 7 分类

本项目的最终问题不是“这 7 个类别谁赢了”，而是分别判断三种物质是否存在：

- `TC` 是否存在
- `OTC` 是否存在
- `CTC` 是否存在

因此主模型应采用“多标签分类”思路，而不是单一 `7` 分类。

推荐输出形式：

- `tc_present`
- `otc_present`
- `ctc_present`

例如：

- `TC only` -> `(1,0,0)`
- `OTC only` -> `(0,1,0)`
- `CTC only` -> `(0,0,1)`
- `TC+OTC` -> `(1,1,0)`
- `TC+CTC` -> `(1,0,1)`
- `OTC+CTC` -> `(0,1,1)`
- `TC+OTC+CTC` -> `(1,1,1)`
- `blank` -> `(0,0,0)`

这样做的原因：

- 更符合化学意义
- 更便于扩展到第二阶段
- 更容易解释模型输出
- 对未知样品的存在性判断更直接

## 4. 第一阶段实验设计

## 4.1 第一阶段核心设定

- 固定总浓度：`60 μM`
- 存在阈值：`10%`
- 最低存在浓度：`6 μM`
- 每种配方做 `3` 个独立重复

这里的“独立重复”指重新配制样品，而不是同一样品重复拍照。

## 4.2 第一阶段样品分组

第一阶段按存在模式分为四类：

- `blank`
- `single`
- `binary`
- `ternary`

### 4.2.1 空白组

- `0 / 0 / 0`

### 4.2.2 单组分组

- `60 / 0 / 0`
- `0 / 60 / 0`
- `0 / 0 / 60`

### 4.2.3 二元混合组

`TC + OTC`：

- `54 / 6 / 0`
- `48 / 12 / 0`
- `42 / 18 / 0`
- `30 / 30 / 0`
- `18 / 42 / 0`
- `12 / 48 / 0`
- `6 / 54 / 0`

`TC + CTC`：

- `54 / 0 / 6`
- `48 / 0 / 12`
- `42 / 0 / 18`
- `30 / 0 / 30`
- `18 / 0 / 42`
- `12 / 0 / 48`
- `6 / 0 / 54`

`OTC + CTC`：

- `0 / 54 / 6`
- `0 / 48 / 12`
- `0 / 42 / 18`
- `0 / 30 / 30`
- `0 / 18 / 42`
- `0 / 12 / 48`
- `0 / 6 / 54`

### 4.2.4 三元混合组

- `48 / 6 / 6`
- `6 / 48 / 6`
- `6 / 6 / 48`
- `36 / 18 / 6`
- `36 / 6 / 18`
- `18 / 36 / 6`
- `6 / 36 / 18`
- `18 / 6 / 36`
- `6 / 18 / 36`
- `20 / 20 / 20`

### 4.2.5 第一阶段总规模

- 空白：`1` 种
- 单组分：`3` 种
- 二元混合：`21` 种
- 三元混合：`10` 种

合计：

- `35` 种唯一配方
- 每种 `3` 个独立重复
- 共 `105` 个样品记录

## 5. 图像采集要求

为了保证颜色特征可比性，拍摄条件必须尽可能固定：

- 黑匣子内拍摄
- 同一手机
- 同一曝光
- 同一焦距
- 同一拍摄距离
- 同一背景条件
- 比色皿放置方式尽量一致

当前拍摄体系已经满足较高标准化，因此第一阶段后续分析可优先使用原始颜色统计，并在此基础上派生归一化与缓冲差值特征。

注意：

- 虽然模型输入可直接使用原始 `RGB`，但建议保留空白图像或空白样品，便于后续排查底色漂移
- 若后续发现不同批次之间存在系统性偏移，可再考虑做空白校正或颜色归一化

## 6. 自动颜色特征提取程序的定位

本项目后续需要一个自动程序来替代人工取色。

程序职责建议限定为：

- 批量读取原始照片
- 自动识别比色皿位置
- 正式保存比色皿裁剪图
- 自动标记异常裁剪结果，供人工修正
- 在裁剪图上标注液体核心取色区域
- 计算该区域的 `RGB / HSV / Lab` 统计
- 输出标注预览图
- 输出结果表

当前不要求程序自动解析样品类别、缓冲类型、浓度信息，这些可以后期手动补充。

### 6.1 为什么不直接取整个比色皿平均色

不建议直接对整个比色皿框求平均颜色，原因如下：

- 比色皿边框存在明显高亮反光
- 顶部液面弯月面会拉偏颜色
- 底部反射和边缘高亮不代表样品主体颜色

因此应优先截取液体中部或中下部的稳定核心区域作为取色区。

当前程序实现中，这个取色区已经进一步明确为：

- 不在原图上标注
- 而是在裁剪后的比色皿图上标注

这样可以减少原图大尺寸构图对 ROI 标注稳定性的影响。

### 6.2 自动识别思路

基于当前图像特征，自动识别可以利用以下特点：

- 黑背景下比色皿是明显亮区
- 比色皿形状接近竖直矩形
- 位置和尺寸变化不大
- 红色文字与白色水印虽然显眼，但在位置、长宽比、连通形状上与比色皿差异较大

程序最终建议输出：

- 原始图片路径
- 比色皿外框坐标
- 裁剪图路径
- 取色区域坐标
- `RGB / HSV / Lab` 的均值与标准差
- 标注预览图

当前这一部分已经从“先做 Python 脚本”推进到“提供本地 GUI 工具”阶段。

## 7. 第一阶段当前推荐的数据记录方式

当前不再推荐手工维护 `water_rgb` 这类单列字符串字段，而是以程序导出的结构化结果为准。

推荐保留三层数据：

- 缓冲级结果表：`project_data*/rgb_results/rgb_results.csv`
- 多缓冲长表：`data_processed/buffer_rgb_long.csv`
- 训练宽表：`data_processed/training_features_wide.csv`

其中单缓冲结果表至少包含：

- `sample_name`
- `group_type`
- `replicate_id`
- `tc_conc_uM`
- `otc_conc_uM`
- `ctc_conc_uM`
- `roi_mean_r/g/b`
- `roi_std_r/g/b`
- `hsv_mean_h/s/v`
- `hsv_std_h/s/v`
- `lab_mean_l/a/b`
- `lab_std_l/a/b`

当前默认训练并不会直接使用所有导出列，而是优先使用：

- `RGB 原最优特征`
  即 `mean + std + 归一化 + 缓冲差值`
- 再加 `HSV` 原始统计

`Lab` 目前保留在提取层与实验层，便于后续继续验证，但暂不作为默认训练主特征。

## 8. 第一阶段完成后的数据整理

假设第一阶段拍照和原始记录已经完成，后续处理建议按以下顺序进行。

### 8.1 颜色特征整理

当前实际流程不再从 `water_rgb` 这类字符串列拆分，而是直接读取程序导出的数值特征列。

整理顺序建议为：

1. 读取各缓冲的 `rgb_results.csv`
2. 按 `sample_name + replicate_id` 对齐
3. 生成多缓冲宽表
4. 在宽表中追加派生特征

当前已验证的派生特征路线包括：

- `RGB mean/std`
- `RGB` 归一化比例
- 缓冲间 `RGB mean` 差值
- `HSV` 原始统计

其中默认训练方案已经切换为：

- `RGB 原最优特征 + HSV 原始统计`

### 8.2 生成存在性标签

根据浓度自动生成三列标签：

- `tc_present`
- `otc_present`
- `ctc_present`

判定规则：

- 浓度 `>= 6 μM` -> `1`
- 浓度 `< 6 μM` -> `0`

### 8.3 生成组合标签

为了后续作图和汇总，可额外生成一列：

- `label_combo`

例如：

- `blank`
- `TC`
- `OTC`
- `CTC`
- `TC+OTC`
- `TC+CTC`
- `OTC+CTC`
- `TC+OTC+CTC`

该列主要用于可视化与汇报，不作为主模型的唯一目标。

## 9. 数据质控与注意事项

在正式训练模型之前，必须先做数据质控。

### 9.1 RGB 格式检查

需要确认：

- 每个 `rgb` 字段都能正确拆成 `3` 个数值
- 不存在空值
- 不存在缺项、错项、非数值字符

### 9.2 重复稳定性检查

同一 `sample_name` 下的 `R1 / R2 / R3` 应尽量接近。

若发现某一重复明显偏离另外两个，应优先回查：

- 原始图片
- 取色区域是否偏移
- 是否存在气泡、液面倾斜、明显反光
- 是否发生曝光异常

### 9.3 空白组稳定性检查

空白组应具备较好的集中性。如果空白本底在多缓冲条件下漂移很大，说明实验体系或拍摄体系还存在不稳定因素。

### 9.4 自动识别结果抽查

建议抽查每组中的部分标注预览图，确认程序确实取到了液体核心区域，而不是：

- 比色皿边框
- 顶部弯月面
- 高光反射区
- 图片中的文字区域

## 10. 第一阶段探索性分析

在上模型之前，先做探索性分析。其目的不是追求最好分数，而是回答：

“多缓冲联合颜色特征是否真的能把不同存在模式拉开？”

建议进行以下分析。

### 10.1 重复稳定性分析

观察同一配方的 `3` 个重复在 `12` 维特征上是否接近。

若重复之间离散度很大，说明体系噪声较高，模型效果即使看起来不错，也可能不稳。

### 10.2 单特征分布分析

逐一观察不同特征对组别的区分能力，例如：

- `water_R`
- `tris6_G`
- `tris7_B`
- `tris8_B`

用于初步寻找敏感通道和敏感缓冲。

### 10.3 PCA 等降维可视化

将多缓冲联合颜色特征投影到二维空间，观察：

- 单组分之间是否分开
- 二元混合是否形成合理分布
- 三元混合是否有相对稳定区域
- 空白是否与样品明显分离

### 10.4 各组平均颜色图谱

按 `label_combo` 或按 `tc_present / otc_present / ctc_present` 分组，绘制平均 `RGB / HSV` 图谱，用于总结规律。

## 11. 第一阶段模型训练方案

## 11.1 主模型思路

第一阶段不建议直接做深度学习。由于样本量较小，更适合小样本监督学习。

推荐采用“三个并行二分类器”的结构：

- 模型 A：判断 `TC` 是否存在
- 模型 B：判断 `OTC` 是否存在
- 模型 C：判断 `CTC` 是否存在

输入相同，均为多缓冲联合颜色特征。

### 11.2 推荐模型

优先尝试以下模型：

- `Logistic Regression`
- `Random Forest`

推荐原因：

- `Logistic Regression` 适合做线性基线模型，稳定、易解释
- `Random Forest` 可以捕捉非线性关系，通常对颜色判别更有弹性

两者若结果一致，说明判别较可靠。

### 11.3 为什么不建议 7 分类作为主模型

虽然也可以把样品直接看作：

- `blank`
- `TC`
- `OTC`
- `CTC`
- `TC+OTC`
- `TC+CTC`
- `OTC+CTC`
- `TC+OTC+CTC`

但这不是最优主方案。原因是：

- `TC+OTC` 与 `TC+OTC+CTC` 并非完全独立概念
- 多标签结构更符合实际问题
- 第二阶段扩展到不同总浓度时，多标签方式更自然

### 11.4 当前已实现的基线训练骨架

为了先跑通从 `RGB` 结果表到模型训练的完整流程，当前已经补充了一套轻量建模骨架，职责分为三层：

- 单缓冲结果层：各缓冲各自输出 `rgb_results.csv`
- 多缓冲整理层：读取多个 `rgb_results.csv`，生成长表、宽表与质控表
- 基线训练层：读取宽表，输出 `tc_present / otc_present / ctc_present` 的多标签判别结果

当前新增的关键文件包括：

- `data_sources/buffer_sources.json`
- `data_processed/buffer_rgb_long.csv`
- `data_processed/training_features_wide.csv`
- `data_processed/training_qc_summary.csv`
- `run_modeling_pipeline.py`
- `启动模型训练.ps1`

其中默认训练特征当前采用：

- `RGB 原最优特征`
  即 `mean + std + 归一化 + 缓冲差值`
- `HSV` 原始统计

当前默认四缓冲方案共 `78` 个输入特征。

### 11.5 当前默认训练结果与颜色特征实验结论

当前默认训练结果位于：

- `model_runs/20260603_203318/model_metrics.json`
- `model_runs/20260603_203318/predictions.csv`

本次训练设置为：

- 输入缓冲：`water`、`T=8`、`T=5`、`T=10`
- 数据切分：按 `sample_name` 分组 hold-out
- 训练行数：`84`
- 测试行数：`21`
- 模型形式：one-vs-rest 的简单 `Logistic Regression` 基线
- 默认特征组：`RGB 原最优特征 + HSV 原始统计`

单次默认训练结果主要用于确认主流程与默认特征组已经落地。

当前这一轮四缓冲单次默认训练结果为：

- `macro_f1 = 0.7848`
- `exact_match_ratio = 0.3333`
- `hamming_loss = 0.2698`
- `TC` 标签 `F1 = 0.7647`
- `OTC` 标签 `F1 = 0.9231`
- `CTC` 标签 `F1 = 0.6667`

由于单次 hold-out 会随切分结果波动，当前这组数值主要用于记录“四缓冲默认训练已经跑通且可复现”，不应直接替代多次随机切分实验的平均比较结论。

更稳妥的特征比较应以：

- `model_runs/color_feature_experiments_20260601_113537/summary.json`

中的多次随机切分平均结果为准。

当前最关键的实验结论是：

- 纯 `RGB` 条件下，最优组合是 `mean + std + 归一化 + 缓冲差值`
- 在三缓冲设定下，再加入 `HSV` 原始统计后，平均 `macro_f1` 提升到 `0.8773`
- 在三缓冲设定下，`exact_match_ratio` 提升到 `0.5984`
- `Lab` 目前没有带来稳定正收益，因此仅保留在提取层与实验层

## 12. 训练集与验证集划分原则

这一部分非常关键，不能简单随机划分。

### 12.1 禁止重复泄漏

同一 `sample_name` 的不同重复不应分别落入训练集和测试集。

例如：

- `binary_TC48_OTC12_CTC0` 的 `R1`
- `binary_TC48_OTC12_CTC0` 的 `R2`
- `binary_TC48_OTC12_CTC0` 的 `R3`

应被视为同一配方组。

### 12.2 正确做法

应按 `sample_name` 分组进行交叉验证，也就是使用“按配方分组”的验证方式。

这样测试集中的样品配方对于模型来说是“未见过的配方点”，更接近真实泛化能力。

## 13. 模型评价指标

建议每个标签模型分别输出以下指标：

- `accuracy`
- `precision`
- `recall`
- `F1-score`

其中更值得关注的是：

- `precision`：避免误报
- `recall`：避免漏检
- `F1-score`：综合平衡两者

建议第一阶段的目标不是盲目追求极高准确率，而是希望三个标签模型都达到稳定、均衡的较好水平。

## 14. 模型解释与判别依据

本项目最终希望达到“模型主判定 + 规则辅助解释”，因此训练完成后不能只停留在分数层面。

### 14.1 特征重要性

应分析哪些 `RGB` 特征对不同标签最重要，例如：

- 某个缓冲下的蓝通道是否对 `CTC` 特别敏感
- 某个缓冲下的红通道和绿通道组合是否对 `TC` 更关键

### 14.2 均值差异分析

可比较：

- `TC 存在` 与 `TC 不存在`
- `OTC 存在` 与 `OTC 不存在`
- `CTC 存在` 与 `CTC 不存在`

在不同缓冲下的平均 `RGB` 差异。

### 14.3 辅助规则提炼

在主模型之外，可以额外用浅层决策树或人工归纳方法，总结辅助判断规律。

例如未来可能形成类似的表达：

- 在某缓冲下蓝通道明显升高，更支持某物质存在
- 不同缓冲之间的相对变化模式更能区分某些组合

这里的规则不一定替代主模型，但适合用于解释、汇报和论文写作。

## 15. 第一阶段是否成功的判断标准

第一阶段建议满足以下三点后，再进入第二阶段：

### 15.1 模型表现稳定

三个标签模型都具有较好的分类性能，不应只靠某一个标签表现突出。

### 15.2 重复一致性良好

同一配方的多个独立重复应保持相对稳定，不应出现大幅漂移。

### 15.3 解释规律与模型结论一致

特征重要性、均值图谱、PCA 分布等应能互相印证，而不是完全依赖黑箱结果。

如果这三点都成立，则说明：

- 探针体系确实具有区分能力
- 多缓冲联合响应是有效信息源
- 可以继续扩展到第二阶段

## 16. 第二阶段扩展思路

第二阶段的目标是让模型适应不同总浓度，而不是改变判别目标。

### 16.1 推荐做法

在保持第一阶段整体分析流程不变的前提下，增加多个总浓度水平，例如：

- `20 μM`
- `40 μM`
- `60 μM`
- `80 μM`

在每个总浓度下继续设计代表性单组分、二元混合、三元混合样品。

### 16.2 第二阶段模型目标不变

仍然输出：

- `tc_present`
- `otc_present`
- `ctc_present`

而不是预测具体浓度。

### 16.3 第二阶段更严格的验证方式

除了按配方分组外，建议增加“留出某个总浓度只做测试”的验证，以判断模型是否真正学会了存在性，而不是记住了某个总浓度的色深。

## 17. 最终未知样品判定流程

当第一阶段和第二阶段都完成后，未知样品的判定流程如下：

1. 取未知样品
2. 在四种缓冲条件下分别与探针体系反应
3. 分别拍照
4. 使用自动程序提取四组 `RGB`
5. 将四组 `RGB` 送入训练好的三个标签模型
6. 分别得到：
   - `P(TC存在)`
   - `P(OTC存在)`
   - `P(CTC存在)`
7. 按设定阈值转为“存在 / 不存在”
8. 输出最终结论

例如：

- `TC：存在`
- `OTC：不存在`
- `CTC：存在`

则结论为：

- 样品中存在 `TC` 与 `CTC`

## 18. 关键注意事项汇总

为避免实验和建模过程中出现偏差，应重点注意以下问题：

- 不要将目标误设为定量，本项目当前阶段的核心是定性存在性判别
- 第一阶段的重点是验证路线是否成立，不必一开始就追求覆盖所有情况
- 不要只依赖单一缓冲，应始终联合多缓冲颜色特征
- 不要只看单张照片，应以结构化数据为分析对象
- 不要让同一配方的重复泄漏到训练集和测试集两边
- 不要直接以整个比色皿区域平均色代替液体核心区域颜色
- 不要仅以模型准确率作为结论，应同时看解释性分析结果
- 若后续发现系统性漂移，应补充空白校正或批次修正
- 不要把横向或纵向位置变化本身直接视为异常，只要比色皿主体已经被稳定裁出，位置漂移未必影响后续 ROI 取色

## 19. 当前建议的执行顺序

按当前讨论结果，推荐的完整执行顺序如下：

1. 完成第一阶段样品配制与拍照
2. 使用本地图像处理工具扫描原图并自动裁出比色皿
3. 修正异常裁剪图
4. 在裁剪图上标注 ROI 并提取当前缓冲下的 `RGB`
5. 逐步补齐其他候选缓冲下的图片与 `RGB` 结果
6. 整理为标准记录表
7. 根据最终采用的缓冲数量，拆分为对应维度特征
8. 生成 `tc_present / otc_present / ctc_present`
9. 进行数据质控
10. 进行探索性分析
11. 训练三个并行分类模型
12. 分析特征重要性与辅助规则
13. 判断第一阶段是否成功
14. 若成功，再扩展到第二阶段多个总浓度
15. 最终建立未知样品判定流程

## 20. 当前方案的核心结论

本项目最核心的结论可以概括为以下几点：

- 利用多种缓冲条件下的联合颜色特征响应，有希望实现 `TC / OTC / CTC` 的定性存在性判别
- 第一阶段应先在固定总浓度 `60 μM` 下做概念验证
- 判别任务应建模为三个并行的“存在 / 不存在”问题
- 自动化图像处理程序已经升级为“两阶段项目流”：
  - 先裁剪比色皿
  - 再在裁剪图上标注 ROI 并提取颜色特征
- 当前默认训练方案已经从纯 `RGB` 进一步切换为 `RGB 原最优特征 + HSV 原始统计`
- 当前默认缓冲体系已经落定为 `water + T=8 + T=5 + T=10` 四体系，工程结构仍保留后续继续扩展缓冲数量的能力
- 第一阶段成功后，再扩展到多个总浓度，才更接近真实未知样品应用

# 项目概览

## 1. 项目定位

本项目用于辅助 `TC / OTC / CTC` 共存体系的定性判别实验，核心任务不是直接做最终分类，而是先把图像处理链路稳定搭起来：

- 从原始实验照片中自动识别比色皿
- 批量裁剪出比色皿图像
- 在裁剪图上标注液体取色区域
- 批量提取颜色特征
- 输出可用于后续建模的结构化结果

当前项目已经从“原图上直接标注 ROI”的第一版思路，升级为“先裁剪比色皿，再在裁剪图上标注 ROI”的两阶段项目流。

## 2. 当前已实现到哪一步

当前已经实现：

- 使用 `Python + Tkinter + OpenCV + Pillow + NumPy + pandas` 搭建本地 GUI 工具
- 支持扫描 `Photo/` 下的实验图片
- 支持按文件名解析 `buffer_name`、`group_type`、浓度和重复编号
- 支持批量自动识别比色皿并保存裁剪图
- 支持自动生成异常待修正列表
- 支持在原图上修正异常裁剪框
- 支持在裁剪图上标注 ROI
- 支持基于裁剪图批量提取 `RGB / HSV / Lab` 颜色统计
- 支持保存和重新打开项目
- 支持保存项目成功弹窗提示与保存路径提示
- 支持识别 `T=8_*`、`T=5_*`、`T=10_*` 这类带 `=` 的缓冲命名
- 支持提取 `RGB / HSV / Lab` 颜色统计
- 支持将不同缓冲的 `rgb_results.csv` 整理为统一训练表
- 支持按 `sample_name` 分组切分训练/测试集
- 支持运行一个简单的多标签基线训练流程，当前默认特征组合为 `RGB 原最优特征 + HSV 原始统计`
- 支持通过 `启动模型训练.ps1` 便捷重跑训练

当前主流程已经可以实际使用，但仍属于第一轮可用版本，后续还可以继续做交互优化和建模模块扩展。

当前已经实际跑通四缓冲闭环：

- `water` 数据位于 `project_data/`
- `T=8` 数据位于 `project_data_t8/`
- `T=5` 数据位于 `project_data_t5/`
- `T=10` 数据位于 `project_data_t10/`
- 训练整理结果位于 `data_processed/`
- 当前默认训练结果位于 `model_runs/20260603_203318/`
- 颜色特征融合实验结果位于 `model_runs/color_feature_experiments_20260601_113537/`

## 3. 当前推荐使用流程

建议按下面顺序操作：

1. 在 GUI 中选择原图目录 `Photo/`
2. 创建项目，项目目录建议使用 `project_data/`
3. 进入“阶段1 原图裁剪”，批量自动裁剪全部比色皿
4. 进入“阶段2 异常修正”，只处理异常待修正列表中的图片
5. 进入“阶段3 ROI 取色”，在裁剪图上标注 ROI
6. 保存 ROI 配置
7. 批量提取颜色特征
8. 在 `project_data/rgb_results/` 中查看结果表，并在 GUI 中查看 `RGB + HSV` 摘要与预览图

## 4. 当前目录与文件作用

### 4.1 根目录

- `Photo/`
  原始实验照片目录，当前按 `blank`、`single`、`binary`、`ternary` 分组存放。

- `project_data/`
  正式项目目录，保存项目状态、裁剪图、预览图和颜色特征结果。

- `project_data_t8/`
  `T=8` 缓冲的正式项目目录，结构与 `project_data/` 相同，用于单独保存第二个缓冲的裁剪图、预览图与颜色特征结果。

- `project_data_t5/`
  `T=5` 缓冲的正式项目目录，结构与 `project_data/` 相同，用于单独保存第三个缓冲的裁剪图、预览图与颜色特征结果。

- `project_data_t10/`
  `T=10` 缓冲的正式项目目录，结构与 `project_data/` 相同，用于单独保存第四个缓冲的裁剪图、预览图与颜色特征结果。

- `data_sources/`
  多缓冲训练流程的输入配置目录，当前用于登记各缓冲的 `rgb_results.csv` 路径。

- `data_processed/`
  多缓冲整理后的长表、宽表与质控表输出目录。

- `model_runs/`
  训练结果目录，保存默认训练结果、颜色特征实验结果、预测表与实际使用的特征列。

- `src/`
  工具源代码目录。

- `docs/`
  设计文档目录。

- `实验方案.md`
  实验与建模方案主文档。

- `项目概览.md`
  当前这份项目交接与复用说明。

- `run_app.py`
  Python 启动入口。

- `run_extract_buffer.py`
  面向命令行的单缓冲批处理入口，用于将指定缓冲从 `Photo/` 提取为正式项目目录。

- `run_modeling_pipeline.py`
  多缓冲整理与基线训练入口。

- `启动工具.ps1`
  Windows 下的启动脚本。

- `启动模型训练.ps1`
  Windows 下的模型训练启动脚本，支持直接覆盖最近一次训练结果。

- `environment.yml`
  Conda 环境说明文件。

### 4.2 `project_data/`

- `project_data/project.json`
  项目主文件，保存样本信息、裁剪状态、异常状态和 ROI 配置。

- `project_data/cropped_cuvettes/`
  正式保存的比色皿裁剪图。

- `project_data/previews/`
  ROI 标注预览图。

- `project_data/rgb_results/rgb_results.csv`
  颜色特征提取结果表，当前会输出 `RGB / HSV / Lab` 统计。

### 4.4 `rgb_results.csv` 列名含义

当前导出的 `project_data/rgb_results/rgb_results.csv` 主要字段含义如下：

- `image_path`
  原始图片的相对路径，对应 `Photo/` 下的原图。

- `crop_path`
  比色皿裁剪图的相对路径，对应 `project_data/cropped_cuvettes/` 下的文件。

- `buffer_name`
  缓冲名称，例如 `water`，后续也可能是其他缓冲。

- `group_type`
  样本分组类型，当前取值为 `blank`、`single`、`binary`、`ternary`。

- `sample_name`
  规范化样本名称，例如 `binary_TC48_OTC12_CTC0`。

- `replicate_id`
  重复编号，当前命名规则下通常为 `R1`、`R2`、`R3`。

- `tc_conc_uM`
  当前样本中 `TC` 的浓度，单位为 `μM`。

- `otc_conc_uM`
  当前样本中 `OTC` 的浓度，单位为 `μM`。

- `ctc_conc_uM`
  当前样本中 `CTC` 的浓度，单位为 `μM`。

- `roi_mean_r`
  ROI 取色区域内红色通道的平均值。

- `roi_mean_g`
  ROI 取色区域内绿色通道的平均值。

- `roi_mean_b`
  ROI 取色区域内蓝色通道的平均值。

- `roi_std_r`
  ROI 取色区域内红色通道的标准差，用于辅助判断颜色是否稳定。

- `roi_std_g`
  ROI 取色区域内绿色通道的标准差。

- `roi_std_b`
  ROI 取色区域内蓝色通道的标准差。

- `hsv_mean_h` / `hsv_mean_s` / `hsv_mean_v`
  ROI 取色区域在 `HSV` 颜色空间中的平均值。

- `hsv_std_h` / `hsv_std_s` / `hsv_std_v`
  ROI 取色区域在 `HSV` 颜色空间中的标准差。

- `lab_mean_l` / `lab_mean_a` / `lab_mean_b`
  ROI 取色区域在 `Lab` 颜色空间中的平均值。

- `lab_std_l` / `lab_std_a` / `lab_std_b`
  ROI 取色区域在 `Lab` 颜色空间中的标准差。

- `pixel_count`
  ROI 原始像素总数，即进入统计前的总像素数。

- `kept_pixel_count`
  经过亮度裁剪后实际参与均值和标准差计算的像素数。

- `status`
  当前图片处理状态，常见为 `ok` 或 `failed`。

- `warning`
  警告信息，例如 `RGB标准差偏高`、`有效像素保留比例偏低`，用于提示这张图需要额外关注。

- `roi_profile_name`
  本次提取 RGB 时使用的 ROI 配置名称。

- `preview_path`
  标注预览图的相对路径，对应 `project_data/previews/` 下的文件。

- `error_message`
  如果该图处理失败，这里记录失败原因；处理成功时通常为空。

### 4.3 `src/distinguish_tc/`

- `models.py`
  数据结构定义，如样本、裁剪记录、ROI 配置、结果记录、项目状态。

- `parser.py`
  文件名解析与图片扫描逻辑。

- `detector.py`
  自动识别比色皿框的逻辑。

- `image_io.py`
  图片读写封装，兼顾中文路径读取。

- `processor.py`
  自动裁剪、异常标记、刷新裁剪图、颜色特征提取、结果导出等处理流程。

- `project_store.py`
  项目状态的保存与读取。

- `calibration.py`
  ROI 配置与比例坐标转换。

- `gui.py`
  Tkinter 界面主逻辑。

## 5. 设计过程中的关键演进

### 5.1 第一版思路

最初方案是：

- 在原图上框定比色皿
- 直接在原图上标注 ROI
- 批量提取 RGB

这个方案能跑通，但后面很快发现一个问题：真正稳定的对象其实不是原图，而是“裁出来的比色皿图”。

### 5.2 改成两阶段项目流

后续明确采用的新流程是：

- 原图批量自动裁出比色皿
- 异常图单独修正
- 在裁剪图上标注 ROI
- 基于裁剪图提取颜色特征

这样做的好处：

- ROI 标注更直观
- 抽查更快
- 位置坐标更稳定
- 更适合以后多缓冲扩展
- 项目状态可以正式保存和复用
- 可以在同一套裁剪图上复用不同颜色空间特征实验

## 6. 开发过程踩过的坑

### 6.1 原图直接标 ROI 不够稳

问题：

- 原图尺寸大
- ROI 标注不够直观
- 原图坐标和取色目标之间隔着一个“比色皿定位”问题

解决：

- 改为“先裁剪比色皿，再在裁剪图上标 ROI”

### 6.2 自动裁剪和 ROI 混在一起，用户心智混乱

问题：

- 如果裁剪框还不准，就去标 ROI，会导致后续结果无意义

解决：

- 界面拆成三个阶段：
  - 阶段1 原图裁剪
  - 阶段2 异常修正
  - 阶段3 ROI 取色

### 6.3 需要正式保存裁剪图，而不是只做临时中间结果

问题：

- 如果每次重新打开都重新裁剪，人工修正就无法复用

解决：

- 正式引入 `project_data/`
- 保存 `project.json`
- 保存 `cropped_cuvettes/`

### 6.4 中文路径读图存在潜在风险

问题：

- Windows 下直接使用 `cv2.imread` 读取中文路径并不总是稳定

解决：

- 在 `image_io.py` 中改为使用 `np.fromfile + cv2.imdecode`

### 6.5 `启动工具.ps1` 曾经出现引号损坏

问题：

- 启动脚本里一行提示文字的引号损坏，导致 PowerShell 报“字符串缺少终止符”

解决：

- 重写 `启动工具.ps1`
- 使用简单稳定的 ASCII 提示文本，避免脚本解析异常

### 6.6 保存项目时无明确反馈

问题：

- 点击“保存项目”后只有状态栏更新，用户不容易确认是否成功

解决：

- 保存成功后弹出 `showinfo`
- 明确显示保存成功和保存路径

### 6.7 临时验证目录容易和正式项目目录混淆

问题：

- 开发过程中曾生成 `results/`、`project_data_validation/` 这类临时目录

解决：

- 已清理旧目录
- 现在正式使用 `project_data/`

### 6.8 第二类缓冲文件名带 `=`，旧解析规则无法直接接入

问题：

- 原先文件名解析默认缓冲名只包含字母和数字
- `T=8_blank.jpg`、`T=5_blank.jpg`、`T=8_binary_48,12,0.jpg` 这类命名在旧规则下无法直接识别

解决：

- 放宽 `parser.py` 中的缓冲名匹配规则
- 让 `T=8`、`T=5` 这类实际实验命名可以直接进入现有流程

### 6.9 把位置漂移误判为异常，会干扰实验设计里的重复性

问题：

- 当前实验中，部分横向或纵向位置变化是有意引入的重复性设计
- 如果程序把位置变化本身直接标为异常，会把“故意变化”误认为“裁剪失败”

解决：

- 自动异常规则不再因横向位置变化或纵向位置变化直接报错
- 只保留更像真裁剪风险的规则，例如尺寸异常、贴近上下边界、检测置信偏低

### 6.10 训练闭环需要从 GUI 主流程中独立出来

问题：

- 现有 GUI 负责单缓冲颜色特征提取，但并不直接负责多缓冲拼表和训练
- 如果把训练逻辑硬塞进 GUI，后续维护会变混乱

解决：

- 新增 `src/distinguish_tc/modeling/` 作为独立建模层
- 通过 `data_sources/`、`data_processed/`、`model_runs/` 把“单缓冲结果”和“多缓冲训练”解耦
- 增加 `run_modeling_pipeline.py` 与 `启动模型训练.ps1` 作为单独入口

## 7. 如果以后让 AI 重新实现，建议保持的核心原则

新的 AI 如果要复现这个项目，建议保持以下原则：

- 不要回退到“原图直接标 ROI”的旧方案
- 一定要保留项目制目录和 `project.json`
- 裁剪图必须正式保存
- ROI 必须相对裁剪图保存
- 异常图必须单独维护状态
- 文件名解析规则必须保留当前约定
- 文档和项目内路径都尽量使用相对路径

## 8. 当前实验与软件的衔接关系

软件层面当前主要解决的是：

`原图 -> 比色皿裁剪图 -> ROI -> 颜色特征结果表`

实验层面后续要做的是：

- 继续补齐其他缓冲条件下的照片
- 将多个缓冲下的颜色特征结果按 `sample_name + replicate_id` 对齐
- 构建多缓冲联合特征
- 做 `TC / OTC / CTC` 的存在性判别模型

当前这条衔接链路已经至少用 `water + T=8 + T=5 + T=10` 实际跑通了一轮：

`Photo/` -> `project_data/`、`project_data_t8/`、`project_data_t5/` 与 `project_data_t10/` -> `data_processed/` -> `model_runs/20260603_203318/`

## 9. TODO：后续界面和程序升级方案

### 9.1 GUI 交互升级

- 支持保存裁剪框后立即弹出成功提示
- 支持保存 ROI 配置后弹出成功提示
- 支持“上一张 / 下一张异常图”快捷切换
- 支持异常图过滤条件切换
- 支持显示更详细的异常原因统计
- 支持更清晰的当前阶段引导提示

### 9.2 裁剪能力升级

- 优化自动裁剪算法，降低误报异常图数量
- 增加对不同批次位置漂移的鲁棒性
- 支持更细的裁剪质量评分
- 支持单图重跑自动检测

### 9.3 ROI 与取色升级

- 支持单图单独覆盖 ROI
- 支持多套 ROI 配置切换
- 支持更丰富的像素过滤策略
- 支持输出更多颜色特征，如 HSV、Lab

### 9.4 项目管理升级

- 支持最近项目列表
- 支持项目备注
- 支持项目内操作日志
- 支持导出项目摘要报告

### 9.5 建模模块升级

- 支持读取多个缓冲结果并自动拼表
- 支持自动生成 `tc_present / otc_present / ctc_present`
- 支持探索性分析图表
- 支持 `Logistic Regression` 和 `Random Forest` 基线训练
- 支持特征重要性导出

### 9.6 工程化升级

- 支持打包为独立桌面程序
- 支持更完整的错误日志文件
- 支持基础自动化测试
- 支持更稳定的中文界面文本校验

## 10. 当前结论

### 10.1 当前四缓冲训练结论

截至当前版本，项目还额外完成了两件关键事：

- 已经从 `water + T=8 + T=5` 扩展到 `water + T=8 + T=5 + T=10` 的四缓冲闭环
- 已经用真实导出的四缓冲数据跑通了一次简单的多标签模型训练

当前默认训练方案与结果表明：

- 流程闭环已经打通
- 默认训练方案已经切换为 `RGB 原最优特征 + HSV 原始统计`
- GUI 当前展示与主流程保持一致，只突出 `RGB + HSV`
- `TC` 与 `OTC` 仍有初步区分力
- `CTC` 仍是当前最弱标签
- `T=10` 项目中的 `RGB` 标准差异常样本已重新修正并重新导出，当前 `project_data_t10/rgb_results/rgb_results.csv` 无警告项
- 当前默认训练结果目录为 `model_runs/20260603_203318/`
- 下一阶段更值得继续补缓冲、做边界样本分析，而不是急着更换复杂模型

当前默认四缓冲单次训练结果为：

- 输入缓冲：`water`、`T=8`、`T=5`、`T=10`
- 训练表规模：`105` 行、`111` 列
- 默认特征数：`78`
- `macro_f1 = 0.7848`
- `exact_match_ratio = 0.3333`
- `hamming_loss = 0.2698`

在后续特征实验中，已经额外验证：

- 纯 `RGB` 条件下，当前最优组合是 `mean + std + 归一化 + 缓冲差值`
- 在此基础上再加入 `HSV` 原始统计后，默认训练方案进一步切换为 `RGB 原最优特征 + HSV 原始统计`
- 按 `model_runs/color_feature_experiments_20260601_113537/summary.json` 的 `30` 次随机切分平均结果看，三缓冲 `rgb_plus_hsv_raw` 的 `macro_f1 = 0.8773`、`exact_match_ratio = 0.5984`
- `Lab` 当前仍保留在提取层与实验层，便于后续继续做特征研究，但默认训练和 GUI 展示都不再优先使用它

当前项目已经不再只是一个“临时取色脚本”，而是一个可以继续扩展的本地项目制图像处理工具。

现在最重要的事情不是推翻重写，而是：

- 继续用当前 `project_data/` 跑通实际实验数据
- 累积多缓冲结果
- 再进入后续建模与判别阶段
